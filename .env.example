# Backend only. Never put this key in Angular or Vercel frontend variables.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
CORNERSCOUT_ORIGINS=http://localhost:4200
# Optional portable data/artifact directories; defaults are repository-local.
# CORNERSCOUT_DATA_DIR=./data
# CORNERSCOUT_ARTIFACTS_DIR=./artifacts
